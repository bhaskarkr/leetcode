/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
/*
    TC - O(log(n))
    SC - O(1)
    Steps:
        1. initialize "closest" with root->val
        2. while root != NULL
            a. check if abs(closest - target) > abs(root->val - target) 
            b. if yes update the closest
            c. for next iteration, set root to left if root->val < target otherwise root->right
        3. return closest
*/
class Solution {
public:
    int closestValue(TreeNode* root, double target) {
        int closest = root->val;
        while(root) {
            if(abs(closest - target) > abs(root->val - target))
                closest = root->val;
            root = root->val > target ? root->left : root->right;
        }
        return closest;
    }
    /*
    int closestValue(TreeNode* root, double target) {
        int ans = root->val;
        TreeNode* child = ans > target ? root->left : root->right;
        if(!child)
            return ans;
        int closest = closestValue(child, target);
        return abs(ans - target) < abs(closest - target) ? ans : closest;
    }
    */
};
