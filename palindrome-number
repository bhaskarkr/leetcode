class Solution {
public:
    bool isPalindrome(int x) {
        int y = 0;
        if(x % 10 == 0)
            return x == 0;
        while(x > 0 && x > y) {
            y = y*10 + x%10;
            x/=10;
        }
        return x == y || x == y/10;
    }
};
